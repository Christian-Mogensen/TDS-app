const ButtonSubmit = ({children, onClick}) => <button onClick={onClick} className="bg-green-500 w-full py-3 rounded self-end text-white shadow-sm" type="submit">{children}</button>
    

export default ButtonSubmit
